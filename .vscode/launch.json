{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ Program",
            "type": "cppvsdbg",  // Use this for C++ programs
            "request": "launch",
            "program": "${workspaceFolder}/a.exe",  // Ensure the executable is named a.exe
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true
        }
    ]
}
